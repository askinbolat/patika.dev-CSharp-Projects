using System;

namespace algorithm
{
    internal class Program
    {
        static void Main(string[] args)
        {
            RemoveCharacter newRemove = new RemoveCharacter();
            newRemove.Remove();
        }
    }
}